# GitHub OAuth Configuration
# Create a GitHub OAuth App at: https://github.com/settings/developers

# Client ID - Safe to expose in frontend
VITE_GITHUB_CLIENT_ID=your_github_client_id_here

# Redirect URI - Must match what's configured in GitHub OAuth App
VITE_GITHUB_REDIRECT_URI=http://localhost:5173/auth/callback

# IMPORTANT: The following should ONLY be set in your serverless function environment
# NEVER commit these values or expose them in the frontend!

# Client Secret - Keep this SECRET! Only use in backend/serverless functions
GITHUB_CLIENT_SECRET=your_github_client_secret_here
